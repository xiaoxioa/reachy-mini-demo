# 隐私:体检产物(人脸照片 / 录音 / 测试音频)——不上传
healthcheck/output/
# 隐私:take_snapshot 快照(可能含人脸)——不上传
voice/output/
# 隐私:视觉调试抓拍(房间/人像)——不上传
vision/debug/
# 二进制模型(3.7MB,下载地址见 CALIBRATION.md §9)
vision/models/

# daemon 运行日志(本地排障用)
tools/daemon_logs/

# 唤醒词调研产物(KWS 模型 32MB / 测试 venv / 合成音频 / 脚本草稿)——留磁盘不进库
tools/_kws*
tools/sherpa-onnx-*/
tools/kws_model.tar.bz2
tools/err.txt

# Python 缓存
__pycache__/
*.pyc

# 本地工具配置
.claude/

# M3-c 记忆数据(含用户画像/对话记忆,隐私)
data/

# 本地配置（含 API Key，不上传；请复制 .env.example 创建）
.env
